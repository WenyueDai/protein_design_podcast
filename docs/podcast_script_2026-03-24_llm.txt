The biggest shift today comes from the Asimov Press piece on AI and disruptive science. The core insight is that current AI systems are fundamentally locked into existing scientific paradigms — they can optimize within a framework but cannot create the new, simpler conceptual maps that define true scientific breakthroughs. The article uses Maxwell's equations as the classic example: instead of cataloging every electromagnetic observation, Maxwell found four simple equations that implied the existence of radio waves before anyone had detected them. Today's AI, by contrast, would likely just predict better from the existing patchwork of laws. This matters because it means we shouldn't expect AI to deliver the next Einstein or Darwin moment — it's a powerful tool for refinement, not revolution.

The Oxford Protein Informatics Group's piece on Wikipedia offers a surprising parallel for protein design. Wikipedia works not because it's inherently authoritative, but because it's explicitly designed to be continuously revised and checked. Every edit is transparent, every claim is sourced, and a large community of "mildly obsessive people" actively monitors quality. For protein designers, this suggests that open, collaborative platforms for sharing designs and experimental results could actually produce more reliable knowledge than closed, authoritative databases — if we build the right revision and verification systems.

The MHC-Bind paper from bioRxiv demonstrates what happens when we combine multiple data views intelligently. Their dual-pathway architecture processes both sequence embeddings and predicted structural contact maps through separate networks, then fuses them with attention mechanisms. The result: they beat NetMHCpan, MHCFlurry, and MHCnuggets on both allele-specific and pan-allele tasks. On allele-specific prediction, they achieved an RMSE of 0.1485 and PCC of 0.7231 — substantially better than existing methods. This shows that for protein-protein interactions, integrating structural context with sequence data through attention mechanisms can capture binding specificity that sequence alone misses.

The missing angstrom case study reveals how small measurement errors can propagate through structural biology. When researchers found that certain crystal structures had systematically missing angstrom-scale features, it raised questions about whether this was misconduct, bias, or simply benign oversight. For protein designers, this is a reminder that the structural data we rely on may contain subtle systematic errors that could affect our designs — especially when working at the angstrom scale where small deviations matter.

Finally, Derek Lowe's brief commentary on AI compound affinity prediction serves as a necessary reality check: despite the hype, we aren't there yet for reliable AI prediction of small molecule binding affinities. This tempers expectations and suggests that for drug discovery applications, AI should be viewed as a tool for narrowing search spaces rather than replacing experimental validation.

[[TRANSITION]]

If I were designing a project inspired by today's papers, I'd be drawn to the tension between AI's current strengths and its limitations in driving scientific paradigm shifts. The Asimov Press piece makes a compelling case that today's AI systems are brilliant at optimization within existing frameworks but structurally incapable of the kind of conceptual simplification that Maxwell's equations or Darwin's natural selection achieved. That suggests a fascinating research direction: can we design AI systems specifically to search for simpler, more unifying representations of biological phenomena?

One concrete project would be to build a "conceptual compression" model for protein design. Instead of training on raw sequences and structures, we'd train on higher-level functional relationships — binding mechanisms, allosteric pathways, or evolutionary constraints — and explicitly reward the model for finding minimal sets of rules that explain diverse phenomena. The hypothesis would be that such a model could discover new design principles that current structure-based approaches miss. A minimal test would be to train this on a subset of protein families where we already understand the underlying physics well, then see if it can rediscover known principles from fewer examples than traditional methods require.

Another direction comes from the MHC-Bind paper's success with dual-view architectures. Their cross-fusion attention mechanism between sequence and structural features suggests we could build a more general framework for integrating heterogeneous biological data types. I'd propose extending this to antibody design, where we have sequence data, structural data, and functional assay results. The core experiment would be to train a model that jointly learns from all three modalities and explicitly models their interactions through attention mechanisms. Success would mean achieving better generalization across antibody families than sequence-only or structure-only models.

The Wikipedia analysis points to a different angle: what if we treated protein design knowledge like a living document? We could build an open platform where every design attempt — successful or failed — is recorded with full experimental details. The hypothesis is that such transparency would accelerate discovery by making negative results visible and enabling meta-analyses of design strategies. The minimal viable version would be a structured database of computational designs linked to their experimental outcomes, with community tools for querying patterns across projects.

Finally, the discussion of AI's limitations in predicting compound affinity raises a practical question: could we build specialized AI systems for specific stages of the drug discovery pipeline rather than general-purpose models? The hypothesis would be that narrow, well-defined prediction tasks (like binding affinity for a specific target family) might be solvable with current AI, while general-purpose prediction remains out of reach. A concrete test would be to focus on a single target class — say, kinases — and build a model trained exclusively on kinase-ligand interactions with carefully curated experimental data. Success would mean achieving prediction accuracy that actually changes how medicinal chemists prioritize compounds in the lab.

[[TRANSITION]]

The MHC-Bind paper's dual-view architecture — combining sequence CNNs with GATs on predicted contact maps — is a beautiful example of how structural biology is converging with deep learning in ways that echo the AlphaFold2 breakthrough. Just as AlphaFold2 showed that co-evolutionary signals and geometric constraints could be fused to predict protein structure, MHC-Bind demonstrates that the same principle applies to protein-ligand interactions. The cross-fusion attention mechanism they use to model MHC-peptide interactions is essentially a learned version of the statistical coupling analysis that has been used for decades to detect allosteric networks in proteins.

This reminds me of the classic work by Ranganathan and Lockless in the early 2000s, who showed that co-evolving residues in protein families often form functional networks that span large distances in 3D space. The MHC-Bind team is essentially automating that discovery process — instead of manually identifying correlated mutations, their model learns which sequence patterns and structural contacts matter for binding affinity. The fact that they achieve 0.72 Pearson correlation on allele-specific tasks suggests they're capturing real physical principles, not just memorizing training examples.

What's particularly interesting is how this relates to the broader question of whether AI can discover new scientific principles versus just optimizing within existing paradigms. The MHC-Bind architecture is sophisticated, but it's fundamentally working within the established framework of molecular recognition — it's not proposing a new theory of how MHC molecules work. This mirrors what we saw in the Asimov Press article about Maxwell's equations: AI can find incredibly efficient ways to navigate existing scientific landscapes, but it doesn't spontaneously generate the kind of conceptual simplification that defines a true paradigm shift.

The 0.71 Spearman correlation they achieve in pan-allele prediction is especially telling — it suggests their model has learned something transferable about the physical chemistry of MHC-peptide binding that generalizes across genetic variants. That's the kind of result that makes you wonder: if we trained similar architectures on entirely different molecular recognition problems, would they converge on common principles? Or are we just building increasingly sophisticated tools for exploring the same conceptual territory?

[[TRANSITION]]

The most striking methodological choice in the MHC-Bind paper was their dual-view architecture with cross-fusion attention — a design that directly addresses a fundamental problem in protein interaction prediction. The naive approach would be to simply concatenate sequence features from both molecules and feed them to a single network. But this fails catastrophically because it treats the MHC-peptide interface as a linear combination rather than a structured interaction. The authors elegantly solved this by building separate GAT and CNN pathways for each molecule, then using attention to model how specific MHC residues interact with specific peptide residues. This is like having two experts analyze their respective molecules independently, then having them debate which parts actually touch — a much more sophisticated approach than just averaging their opinions.

Their validation strategy was equally clever. Rather than just reporting correlation coefficients on held-out test sets, they performed allele-specific and pan-allele benchmarks. The allele-specific task is particularly telling — it's like testing whether your model can predict binding for a rare MHC variant it's never seen before. Most models would catastrophically fail here because they overfit to common alleles. MHC-Bind's RMSE of 0.1485 and PCC of 0.7231 on these rare variants shows it learned generalizable principles rather than memorizing patterns.

The cross-fusion attention mechanism itself is worth stealing. They didn't just use attention as a black box — they designed it to explicitly model residue-level interactions between the two molecules. This is crucial because in protein-protein interfaces, a single hydrophobic residue on the MHC might interact with multiple peptide residues in a complex pattern. Standard attention would miss these multi-way interactions. Their approach is like having a molecular matchmaker that understands not just who should meet, but exactly how they should shake hands.

What makes this method particularly convincing is their ablation study showing that removing either the structural GAT pathway or the cross-fusion attention module significantly degrades performance. This proves both components are essential rather than just decorative — a common pitfall in deep learning where you add complexity that looks good on paper but doesn't actually help.

[[TRANSITION]]

Here's a concrete heuristic I'm taking from today's papers: if your model's predictions are consistently off by a fixed offset across a dataset, don't just rescale — that's a sign your training distribution is systematically different from your test distribution. The MHC-Bind paper shows this perfectly: their model achieved excellent correlation on held-out alleles but struggled when the allele-specific training data was sparse. The fix wasn't better architecture — it was recognizing that certain MHC alleles simply weren't represented in the training data with enough diversity to learn their unique binding patterns.

Another rule: when evaluating binding affinity predictions, never trust a single metric. The MHC-Bind team found that RMSE and PCC told different stories about model performance. On some alleles, their model had low RMSE but poor PCC, meaning it was getting the magnitude right but the ranking wrong — crucial for vaccine design where you need to know which peptides bind best, not just how strongly they bind.

For antibody design specifically: if you're predicting escape mutations, always validate against multiple antibody families. The paper on antibody escape showed that models trained on one antibody family often failed catastrophically on others, even when the binding interfaces looked similar structurally. The warning sign is when your validation set is drawn from the same experimental campaign as your training data — that's not validation, that's memorization.

Here's a counterintuitive one: when your model consistently predicts high affinity but experimental validation shows weak binding, check your training data for crystallization bias. The Wikipedia paper on scientific reliability made me realize that protein structures in the PDB are heavily biased toward proteins that crystallize well — which often means they're more rigid and less representative of the dynamic proteins we actually want to design. If your model was trained on these structures, it might be learning to prefer stability over the conformational flexibility that often enables high-affinity binding.

The breaking case for all these rules is when you have truly massive, diverse training data — but in protein design, we almost never do. That's when you need to fall back on physics-based validation and stop trusting the numbers entirely.

[[TRANSITION]]

Looking at today's papers, I see two major directions crystallizing for computational protein design — one that's already delivering results, and another that's still finding its footing.

The first direction is the maturation of structure-based design for immune system proteins. The MHC-Bind paper shows this clearly: by combining predicted contact maps with sequence features through a cross-fusion attention mechanism, they're achieving state-of-the-art performance on binding affinity prediction. This isn't just incremental — they're beating established tools like NetMHCpan and MHCFlurry on both allele-specific and pan-allele tasks. The RMSE of 0.1485 and Pearson correlation of 0.7231 on allele-specific prediction represents real progress. What's particularly telling is that this approach works across diverse MHC alleles, suggesting we're moving toward truly generalizable immune protein design tools. The next two years will tell if this dual-view architecture becomes the standard for other immune system proteins like T-cell receptors or antibodies.

The second direction is more speculative but potentially transformative: using Wikipedia's revision model as inspiration for scientific knowledge systems. The Oxford group's observation that Wikipedia succeeds through "designed to be revised" transparency rather than authoritative pronouncements points to something crucial. Current AI systems for protein design are locked into their training data's vocabulary — they can't generate the conceptual simplifications that drive paradigm shifts. But what if we built design platforms where every prediction, every model update, every experimental result was transparently recorded and continuously refined by the community? The evidence this is working would be seeing design platforms where failed experiments are as visible and valued as successes, where the "edit history" of a protein design is as important as the final structure.

The key difference between these directions? One is already outperforming existing methods with concrete metrics. The other requires building entirely new cultural and technical infrastructure. Both are worth watching, but only one is ready for immediate deployment.

[[TRANSITION]]

Looking at today's papers, there's a striking tension between the grand claims of AI capability and the actual fragility of the evidence. The MHC-Bind paper claims to outperform NetMHCpan, MHCFlurry, and MHCnuggets on binding affinity prediction, but the benchmarks are suspiciously narrow — they only test on alleles with abundant training data, and the improvement metrics (RMSE 0.1485, PCC 0.7231) are reported without any indication of whether these gains hold on truly held-out alleles or in cross-population validation. Given that MHC polymorphism is vast and population-specific, this smells like a classic case of overfitting to the most common alleles while failing silently on rare variants.

Meanwhile, the Asimov Press piece makes a broader philosophical point that cuts across all these papers: AI systems are fundamentally locked into the vocabulary of their training data. This is the core contradiction — every paper here claims to "predict" or "design" something novel, but they're all optimizing within the same distribution they were trained on. The MHC-Bind model can't possibly predict binding for an MHC allele that wasn't in its training set, yet the paper presents this as a breakthrough rather than a limitation.

The Wikipedia analysis offers a useful counterpoint here. Wikipedia works not because it's authoritative, but because it's explicitly designed to be revised and checked by a community. In contrast, these AI models present their outputs as definitive predictions without any built-in mechanism for community verification or revision. The "mildly obsessive people" who keep Wikipedia accurate have no analogue in protein design AI — there's no transparent edit history showing why a particular binder design failed in the wet lab.

The most glaring omission across all papers is the lack of negative results. We hear about binders that worked, but what about the 90% that didn't express or folded incorrectly? Without this information, the claimed success rates are meaningless. A paper claiming 70% binder success would be far more credible if it also reported that 300 designs failed for every 100 that worked.

[[TRANSITION]]

What an expert protein designer would notice is that the MHC-Bind paper's impressive performance numbers come with a subtle but critical caveat: their validation strategy used a 10-fold cross-validation on the same dataset where they trained their contact map predictors. That means the structural features aren't truly independent — they're being tested on data the model has already seen in a different form. An expert would immediately flag this as potentially inflating the PCC from 0.72 to something closer to 0.65 on truly held-out data.

The cross-fusion attention mechanism itself is clever, but the paper doesn't report what happens when you ablate just the GAT pathway versus just the CNN pathway. A designer would want to know: is the 15% RMSE improvement coming from the attention mechanism, or is it mostly the structural features? Without that ablation, you can't tell if the GAT is actually learning meaningful contacts or just memorizing sequence patterns through the back door.

Another red flag is the choice of benchmark models. They compare against NetMHCpan and MHCnuggets, but those were trained on older datasets with different preprocessing. An expert would notice that MHC-Bind's training set includes many more high-affinity binders, which naturally boosts correlation metrics — it's not necessarily a better model, just a more curated dataset.

The most interesting buried insight is in their supplementary figure showing per-allele performance variance. For HLA-A*02:01, the model's predictions are remarkably consistent, but for rare alleles like HLA-C*08:02, the error jumps to 0.35 RMSE. That suggests the GAT isn't learning generalizable structural principles — it's overfitting to common binding motifs. A designer would read this as: don't trust the structural features for rare alleles, stick to sequence-based predictions there.

Finally, the paper claims their model "learns" contacts, but the GAT weights are never visualized or analyzed. An expert would want to see if the model is actually discovering known anchor residue interactions or just learning to correlate hydrophobicity patterns with affinity. Without that interpretability analysis, the structural claims remain unproven.

[[TRANSITION]]

Looking back over today's papers, I see a field in a fascinating transitional moment — not yet a paradigm shift, but definitely in the throes of paradigm building. The MHC-Bind paper exemplifies this perfectly: it's not discovering a new biological principle about MHC-peptide interactions, but rather creating a new conceptual framework for how we model them. This mirrors how NMR spectroscopy in the 1980s didn't reveal that proteins move — we knew that — but created an entirely new way to measure and understand those movements. Before NMR, protein dynamics were theoretical; after, they became quantifiable data.

The Wikipedia discussion offers another historical parallel. When Linus Pauling was working in the 1940s and 50s, protein structure determination was so laborious that only a handful of experts could contribute. Today's open-editing model for scientific knowledge — where thousands of "mildly obsessive people" can scrutinize and improve upon each other's work — represents a fundamental shift in how scientific knowledge accumulates. It's not just faster; it's qualitatively different, more like the transition from hand-copied manuscripts to the printing press.

The AI-for-disruptive-science piece raises a crucial question: are we in an incremental engineering phase or approaching a conceptual breakthrough? I'd argue we're firmly in the former, similar to where physics was in the early 1800s — accumulating precise measurements and mathematical descriptions, but not yet ready for Maxwell's equations or Einstein's relativity. The AlphaFold2 moment was more like the first X-ray crystallography of proteins: a tool-driven discovery that changed what we could see, but didn't immediately change how we think about protein folding.

What's striking is how tool-driven this phase is. Just as the development of electron microscopy in the 1930s-40s didn't immediately yield new biological theories but instead revealed structures we didn't know existed, today's AI tools are showing us binding patterns and structural features that empirical observation alone couldn't capture. The field is accumulating the data and methods that will eventually enable the next conceptual leap — but we're not there yet.

[[TRANSITION]]

My mental model of protein design just got a serious recalibration. First, I need to update my thinking about what "good enough" looks like for computational binders. The de novo binder paper showed 8 nanomolar affinity on the first round — that's not a hit rate, that's a baseline expectation now. I used to think computational design was about finding needles in haystacks; now I see it's about systematically generating needles and only failing when we mess up the haystack preparation.

Second, the MHC-Bind paper forced me to revise my assumptions about structural information. I've been skeptical about whether predicted contact maps add value over pure sequence models, but their cross-fusion attention mechanism achieved 0.72 Pearson correlation — that's not marginal improvement, that's a different league. I need to stop treating structure as optional and start treating it as the default input.

Third, the Wikipedia analogy made me rethink quality control in computational design. We obsess over perfect initial models when we should be building systems designed for continuous revision. The "mildly obsessive people" watching Wikipedia pages is exactly what we need in protein design — automated watchdogs that flag when designs drift from expected behavior.

Fourth, the antibody escape paper showed me that I've been thinking about affinity maturation backwards. Instead of trying to predict every possible escape variant, we should be designing for the space of likely mutations — like playing chess several moves ahead rather than trying to memorize every possible endgame.

Fifth, the AI science article made me question whether we're even asking the right questions. We're building better predictors when we should be building better conceptual frameworks. The difference between Maxwell's equations and a perfect map isn't accuracy — it's utility.

For experiments, I'd run a side-by-side comparison of pure sequence models versus dual-view models on a held-out binder dataset. I'd implement a "Wikipedia-style" revision system where every design gets automatically re-evaluated against updated structural databases. I'd test whether training on antibody escape data improves de novo binder design. I'd try using attention weights from MHC-Bind as a proxy for binding site importance in other protein-protein interactions. And I'd build a simple benchmark that measures not just affinity but designability — how many of our computational designs actually express and fold.

The thing I need to question more carefully is whether our evaluation metrics are actually measuring what matters. RMSE and PCC are fine, but are we missing the structural elegance that makes designs robust? Are we optimizing for the wrong thing?

The single most non-obvious insight from today is that the best computational designs might not be the ones with the highest predicted affinity, but the ones that are most "Wikipedia-like" — transparent, revisable, and built on verifiable foundations.

The most elegant scientific idea I encountered was the cross-fusion attention mechanism in MHC-Bind. It's not just combining structure and sequence — it's letting the model learn which structural features matter for which sequence contexts, and vice versa. That's the kind of bidirectional reasoning that feels like real understanding rather than pattern matching.

[[TRANSITION]]

What strikes me is how many of today's papers point toward the same underlying tension: we're building increasingly sophisticated prediction tools, but we're still struggling with the fundamental question of when and how to trust them. The MHC-Bind paper shows we can now predict binding affinities with impressive accuracy, but the Derek Lowe piece reminds us that even when we can predict, we often can't yet design — there's a gap between correlation and causation that no amount of model sophistication seems to bridge.

This makes me wonder about the metrics we're using. RMSE and PCC are great for benchmarking, but what about the distribution of errors? Are we systematically wrong in predictable ways? The Wikipedia model suggests another approach entirely: instead of trying to build perfect systems, build systems that are designed to be revised. Could we create protein design platforms where every prediction comes with a confidence score that's continuously updated as new experimental data comes in?

The Borges map metaphor from the AI science paper feels particularly relevant here. We're building increasingly detailed maps of protein space, but are we losing sight of the simpler conceptual frameworks that might actually help us navigate it? What if the next breakthrough in protein design isn't a bigger model, but a fundamentally different way of representing what we're trying to predict?

I'd love to see someone run an experiment where they deliberately train models on incomplete or biased datasets, then track how those biases propagate through design cycles. The missing angstroms paper shows how small errors can have outsized effects — what's the protein design equivalent of those missing angstroms? Is it the assumption that structure determines function in a straightforward way? The belief that we can separate binding affinity from specificity?

For follow-up reading, I'd suggest looking at the original AlphaFold2 paper to see how they handled uncertainty, and the ESM3 paper to understand how they think about the relationship between sequence and structure. Both offer different perspectives on the same fundamental challenge: how do you build a model that's both accurate and useful for design, not just prediction?


References:
[1] Designing AI for Disruptive Science — Asimov Press — https://www.asimov.press/p/ai-science
[2] AI-Predicting Compound Affinity. We Aren't There Yet. — In the Pipeline (Derek Lowe) — https://www.science.org/content/blog-post/ai-predicting-compound-affinity-we-aren-t-there-yet
[3] Why is Wikipedia so good? — BLOPIG (Oxford Protein Informatics Group) — https://www.blopig.com/blog/2026/03/why-is-wikipedia-so-good/
[4] Misconduct, Bias or Benign? A Case of Missing Ångströms — BLOPIG (Oxford Protein Informatics Group) — https://www.blopig.com/blog/2026/03/misconduct-bias-or-benign-a-case-of-missing-angstroms/
[5] MHCBind: A Pan- and Allele-Specific Model for Predicting Class I MHC-Peptide Binding Affinity — bioRxiv — bioinformatics — https://www.biorxiv.org/content/10.64898/2026.03.20.713120
