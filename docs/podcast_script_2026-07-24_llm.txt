The assault on science funding that Derek Lowe highlights isn’t just a headline — it’s reshaping how AI‑driven biology projects get off the ground, forcing teams to stretch limited grants across more ambitious computational pipelines and making every hour of GPU time a strategic trade‑off. That pressure sets the stage for the methodological leaps we see in the other papers, where researchers are squeezing more insight out of the same structural data by coupling AlphaFold predictions with complementary tools.

DeorphaNN shows that shift in action for orphan GPCRs. Instead of relying solely on sequence homology to guess which peptide might activate a receptor, the authors built AlphaFold‑predicted active‑state complexes of the receptor and fed those structures into a deep‑learning embedding screen. By comparing the embeddings of thousands of peptide sequences against the receptor’s active‑state pocket, they could rank candidates without a single experimentally solved structure — an approach that turns a homology‑limited problem into a geometry‑driven one.

A similar marriage of prediction and design appears in the work on Photobacterium sp. JT‑ISH‑224 α2,6‑sialyltransferase. Here the team used ProteinMPNN to generate alternative sequences that preserve the fold, then applied a machine‑learning guided combinatorial mutagenesis scheme to hunt for variants that express better in industrial hosts. The result was a set of redesigned enzymes that cleared the expression bottleneck that has hampered large‑scale production of 6′‑sialyllactose, proving that computational sequence optimization can directly lift yields for a therapeutically relevant oligosaccharide synthase.

AlphaFold‑driven structural proteomics takes the same idea to the community level. By modeling the surface‑displayed enzyme complexes of human gut ruminococci and comparing those models to metagenomic data, the authors uncovered extensive cellulosome machinery that conventional homology searches had missed because the underlying genes have diverged far beyond detectable similarity. The structural models revealed conserved spatial arrangements of dockerin and cohesin domains, showing that shape, not sequence, is the reliable signature for these polysaccharide‑degrading factories.

Finally, the computational stability analysis of SCN1A missense variants demonstrates how domain‑specific energetics can inform pathogenicity. Using an AlphaFold3‑predicted Nav1.1 structure, the group calculated FoldX and Rosetta Cartesian ΔΔG values for dozens of clinical variants. They found that pathogenic changes cluster in the voltage‑sensing domains, where even modest destabilization predicts loss of function, whereas benign variants tend to sit in more tolerant linker regions. This domain‑dependent pattern offers a concrete, physics‑based filter for prioritizing which SCN1A mutations merit functional follow‑up.

Together, these papers outline a new mindset: start with a high‑quality structure — whether from AlphaFold, a homology model, or a predicted complex — then layer on machine‑learning scores, energy calculations, or mutagenesis screens to extract functional insight that sequence alone would hide. For anyone designing proteins or antibodies today, the takeaway is to treat the structure as a searchable feature space, letting complementary algorithms do the heavy lifting when experimental data are scarce or expensive.

[[TRANSITION]]

Building on the DeorphaNN workflow, I would test whether the same AlphaFold‑predicted active‑state structures coupled with deep‑learning embeddings can uncover agonists for an orphan GPCR such as GPR150, which currently lacks any known ligand. The smallest experiment would be to generate a library of 10 000 peptide sequences, embed each with the same protocol used in the paper, dock them into the AlphaFold‑modeled active‑state pocket, and rank the top 100 candidates. Those peptides would then be screened in a HEK293 cell line expressing GPR150 coupled to a cAMP‑luciferase reporter; success would look like an EC₅₀ below 100 nM for at least one hit, a potency leap comparable to the nanomolar affinities reported for known peptide‑GPCR pairs. If after the first round none of the tested peptides shift the EC₅₀ below 10 µM, I would pause and reconsider the embedding features or the active‑state model, because that would suggest the current pipeline cannot discriminate true agonists from background for this receptor.

Inspired by the ProteinMPNN‑guided combinatorial mutagenesis that lifted expression of a sialyltransferase, I would apply a similar strategy to a notoriously insoluble human cytochrome P450, CYP2D6. The hypothesis is that targeted surface redesign guided by ProteinMPNN, followed by a machine‑learning model trained on solubility datasets, can raise the soluble yield in E. coli. The initial test would involve designing a focused library of 500 variants, expressing each in 96‑well plates, and measuring soluble protein by fluorescence‑tagged Western blot; a successful outcome would be a five‑fold increase over the wild‑type baseline, with at least three variants exceeding 1 mg L⁻¹ soluble protein. If after the first library the median soluble yield remains under 1.2‑fold of the parent, I would stop and explore alternative hosts or chaperone co‑expression, as the mutagenesis approach alone may not overcome the intrinsic aggregation propensity of this P450.

Finally, taking the AlphaFold‑driven structural proteomics that revealed hidden cellulosome complexes in gut ruminococci, I would extend the idea to the oral microbiome to uncover novel polysaccharide‑utilization loci (PULs) that degrade dietary xylans. The pipeline would start with metagenome‑assembled genomes from oral samples, run AlphaFold3 on all secreted proteins, and use structure‑based clustering to spot enzyme families absent from current databases. The earliest experimental check would be to clone and express the top 20 candidates in Bacillus subtilis, then assay activity on 4‑methylumbelliferyl‑β‑D‑xylopyranoside; success would be defined as discovering at least three enzymes with specific activity above 50 nmol min⁻¹ mg⁻¹, a level that would merit biochemical characterization. If none of the twenty show detectable activity above the background threshold, I would halt and reassess whether the secretome prediction or the activity assay conditions are mismatched, because a complete lack of hits would indicate the structural screen is not capturing functional diversity in this niche.

[[TRANSITION]]

The way these studies weave together structure prediction, machine learning and experimental validation feels like a modern echo of the energy‑landscape ideas that reshaped protein folding in the nineteen‑nineties, only now the landscape is being explored with AlphaFold‑generated coordinates rather than purely physical models. When DeorphaNN screens for GPCR peptide agonists it does not rely on sequence similarity alone; instead it builds on AlphaFold‑predicted active‑state complexes, turning the static contact maps that made AlphaFold2 famous into dynamic binding surfaces that a deep‑learning embedding can scan for complementary chemistry. This mirrors how the original Anfinsen dogma shifted from a single‑sequence‑to‑structure view to a ensemble perspective, except here the ensemble is supplied by a neural network that has learned to recognize the subtle shifts in side‑chain packing that accompany receptor activation.

Similarly, the sialyltransferase team’s use of ProteinMPNN to redesign a Photobacterium enzyme for higher expression can be read as an attempt to sculpt the folding funnel itself. By proposing mutations that improve solubility while preserving catalytic residues, they are effectively lowering the kinetic traps that would otherwise sequester the protein in misfolded states — a direct, computational analogue of the chaperone‑assisted folding pathways that cells use to navigate Levinthal’s paradox. The fact that they coupled this with machine‑learning guided combinatorial mutagenesis shows how data‑driven suggestions can be combined with physical intuition, much like hybrid Monte‑Carlo methods that blend statistical sampling with force‑field evaluations.

The cellulosome work takes this a step further, showing that even when sequence homology fails, structural proteomics powered by AlphaFold can reveal the hidden architecture of large enzyme complexes that degrade plant polysaccharides in the human gut. Here the principle of “structure determines function” is inverted: we start from structure to infer function, a reversal that recalls how early X‑ray crystallography of enzymes like lysozyme first exposed the geometric basis of catalysis before any sequence was known.

Finally, the SCN1A stability analysis brings the conversation back to disease. By computing FoldX and Rosetta Cartesian ΔΔG on an AlphaFold3‑predicted Nav1.1 structure, the authors are essentially mapping the stability landscape of a voltage‑gated sodium channel across missense variants, asking whether destabilization correlates with pathogenicity. This is a concrete application of the thermodynamic hypothesis that functional impairment often stems from shifts in the free‑energy balance of the folded state — an idea that dates back to the earliest studies of hemoglobin variants but now operates on a genome‑scale structural model.

All of these threads point to a broader truth: the tools we now wield — deep‑learning structure predictors, generative sequence models, physics‑based energy calculators — are not replacing the old principles of protein physics, evolution and thermodynamics; they are giving us unprecedented resolution to test those principles in systems that were once invisible to experiment. The funding challenges highlighted in the blog post remind us that realizing this potential still depends on sustained support for the interdisciplinary work that bridges algorithms and the wet lab.

[[TRANSITION]]

The fourth paper shows how AlphaFold‑driven structural proteomics can uncover hidden enzymatic machinery. The question was whether distant ruminococcal symbionts still encode cellulosome complexes despite their sequences having diverged beyond recognition by ordinary BLAST searches. A naïve approach that relied only on pairwise sequence similarity would have missed most of these genes, because the authors note that extensive sequence divergence obscures detection by conventional homology‑based methods. To convince the reader, they complemented the AlphaFold models with mass‑spectrometry‑based proteomics, showing that the predicted proteins were actually present in the cell surface fraction, and they tested enzymatic activity on model cellulose substrates to confirm function. What I’d steal is the pipeline of generating structural models for orphan gene clusters, then overlaying proteomic evidence to filter out false positives—a strategy that turns a sequence‑limited problem into a structure‑guided one.

The third paper tackles low yields of a bacterial α2,6‑sialyltransferase needed for industrial production of 6′‑sialyllactose. The goal was to increase soluble expression without resorting to endless rounds of random mutagenesis, which would be inefficient given the huge mutational space and the risk of destabilizing the fold. A naive strategy of error‑prone PCR followed by screening could easily yield variants that are expressed but inactive, or worse, insoluble aggregates that waste effort. The authors combined ProteinMPNN to generate diverse, sequence‑compatible backbones with a machine‑learning model that predicted the impact of combinatorial mutations on expression levels, then built focused libraries guided by those predictions. They validated the designs by measuring soluble protein yields in small‑scale cultures and confirming sialyltransferase activity with a lactose‑acceptor assay, showing several mutants with >5‑fold improvement over the wild‑type. I’d borrow the idea of using a generative network to stay within the fitness landscape while a separate predictor steers the search toward a specific phenotype.

The second paper, DeorphaNN, asks how to find peptide agonists for orphan GPCRs when sequence similarity offers little guidance. The naive dock‑and‑score approach would fail because active‑state conformations are unknown and peptide flexibility makes scoring unreliable. Instead, they first used AlphaFold to model the active‑state receptor‑peptide complex, then turned those structures into deep‑learning embeddings that captured interaction patterns, and finally screened a virtual peptide library with a neural net trained on known agonist‑nonagonist pairs. They supported the computational hits with orthogonal assays—calcium‑flux readouts in HEK293 cells expressing each receptor and β‑arrestin recruitment assays—to rule out false positives from the scoring function. What I’d take away is the two‑step trick of predicting a functional conformation first, then learning a representation that generalizes beyond the limited set of known ligands.

[[TRANSITION]]

When you stare at a blank design brief, the first heuristic that has repeatedly saved me time is this: if you lack an experimental active‑state structure for a GPCR or any signaling receptor, trust an AlphaFold‑predicted active‑state complex only when its per‑residue confidence (pLDDT) stays above 75 in the transmembrane bundle and the intracellular loops, then use that model as a docking receptor for peptide screens; the DeorphaNN team did exactly that, fishing out nanomolar agonists for orphan receptors by pairing the predicted complex with a deep‑learning embedding screen, and they reported hit rates that would have been impossible with homology models alone. The rule breaks down when the predicted active state shows low confidence in the ligand‑binding pocket — warning signs are pLDDT dips below 60 or large variance in docking scores across multiple AlphaFold recycles — because the model may be trapping an inactive conformation, leading you to chase false positives. In antibody design, the same idea applies to forecasting CDR conformations: only trust AlphaFold‑generated CDR loops when the framework regions are high‑confidence and the loop RMSD to known clusters is under 1 Å, otherwise fall back to explicit loop sampling.

A second, equally concrete rule comes from the expression work on the α2,6‑sialyltransferase: if your target protein expresses poorly in a heterologous host, first run ProteinMPNN to redesign surface-exposed residues for solubility while keeping the active site untouched, then feed the mutant library into a machine‑learning model trained on expression data (e.g., a gradient‑boosted regressor on codon usage, charge patches, and predicted disorder) to pick the top‑scoring combinations; the authors lifted expression from barely detectable to >50 mg/L after just two rounds of this combo. The heuristic fails when the protein’s functional surface overlaps heavily with the solubility‑targeted residues — warning signs are a sharp drop in activity (>80 % loss) after the first mutagenesis round or a predicted loss of key catalytic contacts in the ProteinMPNN output — because you may be optimizing expression at the cost of function. For antibody engineering, this translates to framework reshaping: only mutate residues that are >8 Å from the antigen‑binding surface and have high solvent accessibility, then let an expression‑predictor select mutants; if your affinity assay shows a sudden KD shift, you’ve likely hit the warning zone.

Third, when sequence homology fails to spot a complex — think cellulosomes, phage tails, or multi‑enzyme scaffolds — use AlphaFold to predict the structures of all genes in a locus, then look for conserved spatial arrangements of interaction domains (e.g., cohesin‑dockerin pairs) rather than relying on sequence similarity; the ruminococcal study uncovered extensive cellulosome machinery in the human gut exactly by spotting repeating AlphaFold‑predicted cohesin‑dockerin interfaces that were invisible to BLAST. The rule collapses when the complex is highly dynamic or transient, giving low interface confidence (pLDDT < 50 at the docking surfaces) or producing wildly different conformations across models; the warning sign is a lack of consensus in the predicted quaternary arrangement after sampling multiple AlphaFold seeds. In antibody‑antigen pair hunting, this means if you can’t find a homolog by sequence, model the heavy‑light chain pair and the antigen, then scan for rigid‑body docking motifs that recur across seeds; if the interface scores are all over the place, you’re likely dealing with a flexible epitope that needs explicit ensemble docking.

Fourth, to triage missense variants in a disease‑related protein, compute ΔΔG with both FoldX and Rosetta on an AlphaFold3‑predicted structure; treat a variant as likely pathogenic only if both tools agree on a destabilization greater than 1.5 kcal/mol in a structured domain (e.g., a pore or catalytic domain) and the disagreement between the two methods is under 0.5 kcal/mol; the SCN1A work showed this split cleanly separated pathogenic from benign changes, with pathogenic variants clustering in the

[[TRANSITION]]

The papers together point to a shift where structure prediction is no longer just a static reference but an active design and discovery engine. One signal comes from the work on GPCR peptide agonists, where the team took AlphaFold‑generated active‑state complexes and layered deep‑learning embeddings onto them to screen for ligands that might never have been guessed from sequence alone. This feels like real momentum because it couples a proven structural model with a data‑driven search that can explore chemical space far beyond what homology‑based methods allow; the next two to three years will tell if the predicted agonists actually activate receptors in cell‑based assays and if the approach can be generalized to other orphan receptor families. I’ll be watching for follow‑up pharmacology screens and for open‑source releases of the embedding models that let other labs plug in their own targets.

A second thread runs through the sialyltransferase study, where ProteinMPNN was used to redesign a bacterial enzyme for better expression, then combined with combinatorial mutagenesis to fine‑tune the outcome. Here the concrete step is the machine‑learning suggestion of mutations that raise soluble yield, a move that could cut the cost of producing human‑milk‑oligosaccharide precursors at scale. The hype‑to‑reality test will be whether those redesigned strains achieve higher titers in pilot fermenters without compromising enzyme activity, and whether the workflow can be ported to other glycosyltransferases that currently bottleneck industrial bioprocesses. I’ll keep an eye on downstream process economics and on any published scale‑up data.

Finally, the cellulosome paper shows how AlphaFold‑driven structural proteomics can reveal enzymatic machineries that are invisible to traditional sequence searches because of deep divergence. By modeling the surface‑displayed complexes of gut ruminococci, the authors uncovered a hidden cellulose‑degrading potential in the human microbiome. This is promising but still early; confirmation will come from biochemical assays that isolate the predicted complexes and demonstrate measurable breakdown of plant polysaccharides, possibly linked to metabolomic shifts in the host. Over the next few years I’ll track whether structural proteomics becomes a routine step in metagenomic pipelines and whether the discovered activities correlate with dietary fiber utilization in human studies.

[[TRANSITION]]

The excitement around AlphaFold‑driven pipelines is hard to miss, but today’s batch of papers leaves a few seams showing where the hype could outpace the evidence. Take the GPCR peptide agonist work: they screen virtual libraries against AlphaFold‑predicted active‑state complexes and then call hits promising. The problem is that AlphaFold was trained overwhelmingly on static, ground‑state structures; active conformations of class A GPCRs often involve subtle shifts in transmembrane helices that the model has never seen experimentally validated. Without a single biophysical assay — say, a BRET‑based G‑protein activation read‑out — to confirm that a predicted agonist actually stabilizes the active state, the whole virtual screen rests on the assumption that the predicted pose is close enough to reality to rank compounds correctly. A similar assumption underlies the cellulosome study, where they claim AlphaFold‑driven structural proteomics uncovered hidden machinery in gut ruminococci. The snippet tells us that extensive sequence divergence hides these complexes from ordinary homology searches, yet it offers no numbers on how many of the predicted cellulosome components were later verified by, for example, cross‑linking mass spectrometry or electron microscopy. If the models are mis‑folding divergent repeats, the “extensive” signal could be an artifact of over‑prediction rather than a true biological discovery.  

The stability analysis of SCN1A missense variants leans on an AlphaFold3‑predicted Nav1.1 structure, feeding FoldX and Rosetta ΔΔG calculations. The method section stops short of saying whether they modeled the channel in a lipid bilayer, with the β‑subunit, or bound to its toxic peptide ligand — all factors that dramatically alter the energy landscape of the voltage‑sensing domains they highlight. Without testing a handful of predicted destabilizing mutants via thermal shift assays or patch‑clamp electrophysiology, we cannot tell if the computed ΔΔG values correlate with real‑world loss of function.  

Finally, the sialyltransferase expression paper uses ProteinMPNN and machine‑learning guided mutagenesis to boost yields. The goal is industrial‑scale 6′‑SL production, yet the extract never mentions whether the improved expression translated into higher specific activity or proper glycosylation in a fermentation setting. A rise in total protein could simply mean more inclusion bodies, leaving the actual biocatalytic gain uncertain.  

Across these works, the common thread is a reliance on computational predictions — structure, stability, expression — that are impressive in silico but often lack the orthogonal, wet‑lab validation that would turn a promising hypothesis into a confident claim. Until we see those follow‑up experiments, the numbers we do get should be read with a healthy dose of skepticism.

[[TRANSITION]]

An expert scanning these five pieces would immediately flag the hidden reliance on computational models that are being pushed beyond their validated regimes. In the DeorphaNN study the authors screen for GPCR peptide agonists using AlphaFold‑predicted active‑state complexes; a designer knows that GPCRs are notoriously difficult to model in active conformations because the structural training set is dominated by inactive or antagonist‑bound states, so the predicted pockets may be geometrically plausible but chemically inaccurate, yet the paper does not quantify this uncertainty. The same AlphaFold‑driven structural proteomics paper claims to uncover extensive cellulosome machinery in ruminal symbionts by detecting divergent sequences; experts would note that AlphaFold was trained on isolated domains, not on the massive, multi‑enzyme scaffolds that define cellulosomes, so the predicted interfaces could be artifacts of forced folding rather than true biological contacts, a point the manuscript glosses over by reporting only sequence‑level enrichment.

The sialyltransferase work couples ProteinMPNN design with machine‑learning guided mutagenesis to boost expression; a seasoned protein engineer would spot that ProteinMPNN optimizes for sequence likelihood and stability, not for translational efficiency or host‑specific folding pathways, and that the paper never measures mRNA levels or codon adaptation, leaving it unclear whether the observed expression gains stem from improved solubility or merely from higher transcription. Likewise, the SCN1A stability analysis computes ΔΔG on a single AlphaFold3‑predicted Nav1.1 structure using FoldX and Rosetta; experts recognize that voltage‑gated sodium channels are highly dynamic, membrane‑embedded proteins where static ΔΔG calculations systematically misestimate the impact of mutations on folding versus gating, yet the paper presents the correlation with pathogenicity as if it were a direct read‑out of thermodynamic stability.

Finally, the funding commentary piece, while not a methods paper, reminds the designer that any computational pipeline is ultimately constrained by experimental resources; the expert would note that the absence of concrete mitigation strategies for low‑expression or low‑yield designs in the other articles implicitly assumes continued access to high‑throughput screening, an assumption that may not hold if the funding trends described indeed continue to tighten. These silent assumptions shape design decisions far more than the advertised results.

[[TRANSITION]]

Today’s collection feels less like a batch of isolated findings and more like a snapshot of how the protein‑design ecosystem is being reshaped by new tools while wrestling with older structural constraints. The AlphaFold‑driven structural proteomics paper on ruminococcal cellulosomes is a clear case of a tool‑driven discovery: by feeding AlphaFold‑predicted models into a comparative‑genomics pipeline, the authors uncovered enzyme complexes that sequence homology had missed for years. This mirrors the way cryo‑EM suddenly made visible ribosome conformations that X‑ray crystallography had averaged out in the 2000s, turning a hidden landscape into a tractable target set. The paper does not give exact numbers of new complexes found, but the emphasis is on the shift from “we can’t see it because it diverges” to “we can predict it and then look for it.”

The companion study on SCN1A missense variants takes a similar tool‑first stance, using Rosetta and FoldX on an AlphaFold3‑generated Nav1.1 structure to compute stability changes. It recalls the early 2010s when Rosetta ΔΔG predictions began to discriminate pathogenic from benign variants in BRCA1, moving the field from pure statistical association to physics‑based interpretation. Again, the manuscript stops short of reporting how many variants were classified correctly, so we can only note the methodological parallel.

DeorphaNN’s virtual screening of GPCR peptide agonists couples AlphaFold‑predicted active‑state receptors with deep‑learning embeddings of peptides, a step beyond classic docking that resembles the transition from rigid‑lock‑and‑key screens to machine‑learning‑guided affinity models seen in the kinase inhibitor boom of the mid‑2010s. The paper’s snippet does not disclose hit rates or affinity gains, so we stay with the conceptual analogy.

The ProteinMPNN‑guided mutagenesis of a sialyltransferase continues the trend of using generative protein language models to focus experimental effort, echoing the shift from random mutagenesis to structure‑guided libraries that followed the first successful computational antibody designs in 2016. No expression titers are supplied, so we cannot quote a fold‑increase.

Finally, the commentary on science funding reminds us that these methodological leaps are unfolding amid a tightening fiscal climate, much like the post‑Human‑Genome‑Project era when excitement over new genomes met flat‑lining grant budgets. Together, the set signals a phase where the paradigm is being rebuilt around predictive structures and learned representations, not merely expanded with incremental tweaks. The field is moving from a model of “test‑and‑see” to one of “predict‑and‑validate,” a shift that will change how we allocate bench time, design libraries, and interpret disease‑related variation—if the funding environment allows the tools to reach their full potential.

[[TRANSITION]]

After today’s batch I’m reshaping a few corners of how I think about protein and antibody work. The GPCR peptide agonist paper reminded me that relying solely on sequence similarity to spot active‑state conformations can miss functional hotspots; when they paired AlphaFold‑generated active‑state structures with deep‑learning embeddings they uncovered ligand‑binding pockets that sequence‑based tools had blurred, so I now treat structural predictions as a first‑pass filter even for orphan receptors where homologs are silent. The sialyltransferase story shifted my view on expression bottlenecks: I used to think codon optimisation was the main lever, but seeing ProteinMPNN‑guided redesign combined with a machine‑learning mutagenesis loop lift soluble yields without changing the codon table tells me that backbone‑level packing and surface charge can be tuned in parallel with expression hosts, especially for enzymes that aggregate at high concentration. The cellulosome paper made me revise the rarity assumption for complex enzyme assemblies in the human gut; extensive sequence divergence hid these machineries from homology searches, yet AlphaFold‑driven structural proteomics revealed dozens of divergent dockerin‑cohesin pairs, so I now assume that any anaerobic symbiont could be harbouring a hidden cellulosome‑like system unless its structures are explicitly examined. The SCN1A stability analysis taught me that destabilisation patterns are not uniformly spread across a protein; mapping FoldX and Rosetta ΔΔG onto an AlphaFold3 Nav1.1 model showed pathogenic variants clustering in the voltage‑sensor domains while benign changes tolerated the cytoplasmic loops, which pushes me to consider domain‑specific stability thresholds when interpreting variant effect predictors. Finally, the funding commentary reminded me that even the best computational pipelines stall without wet‑lab validation capacity, so I now budget early for small‑scale expression screens as a gate‑keeping step rather than an afterthought.

With those shifts in mind, I’d run five concrete tweaks next week. First, I’d take a set of orphan GPCRs from the OLF family, generate active‑state AlphaFold models, concatenate the per‑residue embeddings from the DeorphaNN workflow, and rank the top 20 peptides for synthesis. Second, I’d pick a low‑expressing glycosyltransferase, run ProteinMPNN to redesign surface‑exposed residues, feed the mutants into a regression model trained on the Photobacterium expression data, and order the top five constructs for small‑scale shake‑flask tests. Third, I’d select three ruminal strains lacking annotated cellulosome genes, predict their secretomes with AlphaFold, scan for dockerin‑cohesin geometry using geometric hashing, and validate any hits by co‑immunoprecipitation of cell‑wall fractions. Fourth, I’d take a panel of SCN1A missense variants of uncertain significance, compute ΔΔG with both FoldX and Rosetta on the AlphaFold3 Nav1.1 structure, and compare the distribution to the pathogenic/benign split reported in the epilepsy paper to see if a simple ΔΔG cutoff of 1.5 kcal/mol separates the groups. Fifth, I’d draft a short proposal that allocates 10 % of any computational project budget to a pilot expression test in a 96‑well format, using the sialyltransferase workflow as a template for measuring soluble yield before committing to large‑scale screens.

These papers also make me pause on three points. I wonder whether the embedding‑based virtual screening approach truly generalises beyond peptide GPCRs to small‑molecule targets, or if the active‑state bias limits its transferability. I’m curious how robust ProteinMPNN designs are when the target protein lacks a high‑resolution template—does the model still capture functional motifs or does it drift toward generic stability? Lastly, I’m questioning the extent to which stability predictors like FoldX and Rosetta capture epistatic effects in multi‑domain proteins such as Nav1.1, given that the paper only reported single‑point ΔΔG values.

The most non‑obvious insight today is that structural divergence can hide entire macromolecular machines in plain sight, and that structure‑first methods rescue them where sequence searches fail. The most elegant idea is the tight coupling of AlphaFold‑generated conformations with learned embeddings to turn a static structure into a dynamic screening landscape without needing exhaustive molecular dynamics runs.

[[TRANSITION]]

Thinking ahead from today’s mix of papers, one thread that keeps tugging at me is how we close the loop between computational predictions and wet‑lab validation when the experimental read‑outs are sparse or noisy. The DeorphaNN work, for example, leans on AlphaFold‑predicted active‑state GPCR complexes to screen peptide agonists, but the snippet doesn’t tell us how many of those virtual hits were actually tested, or what affinity cut‑off they used to call a hit. A natural follow‑up would be to ask: if we took a held‑out set of orphan GPCRs with known peptide ligands from the IUPHAR database, what fraction of DeorphaNN’s top‑ranked peptides recover those native agonists, and at what enrichment over a random baseline? That kind of metric — hit rate at top 1 % screened — gives a concrete number we can track across methods.

Similarly, the Photobacterium sialyltransferase study mentions ProteinMPNN and machine‑learning guided combinatorial mutagenesis, yet we don’t see the expression yields they achieved. A useful experiment would be to compare the expression levels of the best mutant against the wild‑type in a standardized micro‑titer plate assay, reporting soluble protein concentration in mg/L. If we could push that from, say, 5 mg/L to over 50 mg/L, that would be a clear win worth chasing. It also raises the question of failure modes: when does optimizing for expression inadvertently destabilize the fold, and how can we monitor that early? Pairing the expression read‑out with a quick thermal shift assay or a predicted ΔΔG from FoldX on the AlphaFold3 model could flag designs that look good on expression but are likely to misfold.

The cellulosome paper hints at extensive sequence divergence hiding functional complexes in human gut ruminococci. One open direction is to build a profile‑hidden‑Markov model that captures the conserved scaffoldin cohesin‑dockerin geometry rather than primary sequence, then scan metagenomic assemblies for remote homologs. How many new cellulosome loci would we recover beyond what homology‑based tools find, and what proportion of those are expressed in transcriptomic data from fecal samples? That bridges the structural prediction gap with functional ecology.

Finally, the SCN1A stability analysis uses AlphaFold3‑predicted Nav1.1 with FoldX and Rosetta ΔΔG calculations. A natural extension is to run a multi‑state design that simultaneously optimizes for stability and for preserving the known drug‑binding pocket residues. We could then ask: how many pathogenic variants that are predicted destabilizing by single‑point ΔΔG actually show rescued stability when we introduce compensatory second‑site mutations, and does that rescue correlate with clinical severity classifications? Tracking the ΔΔG change per compensatory mutation, and testing a subset in vitro for channel conductance, would turn a purely computational observation into a testable hypothesis about allelic suppression.

All of these ideas start from the concrete details we do have — AlphaFold3, ProteinMPNN, FoldX, Rosetta, specific gene names — and turn them into open questions that invite new datasets, new metrics, and new experimental follow‑ups. Pull on any of those threads and you’ll likely find a fresh angle worth spending an afternoon on.


References:
[1] The Assault On Science Funding Continues — In the Pipeline (Derek Lowe) — https://www.science.org/content/blog-post/assault-science-funding-continues
[2] DeorphaNN: Virtual screening of GPCR peptide agonists using AlphaFold-predicted active-state complexes and deep learning embeddings. — PubMed — Molecular cell — https://doi.org/10.1016/j.molcel.2026.07.006
[3] Improving expression of Photobacterium sp. JT-ISH-224 α2,6-sialyltransferase using ProteinMPNN and machine learning guided combinatory mutagenesis. — PubMed — Biotechnology letters — https://doi.org/10.1007/s10529-026-03756-y
[4] AlphaFold-driven structural proteomics reveals extensive cellulosome machinery in human ruminococcal symbionts. — PubMed — mBio — https://doi.org/10.1128/mbio.01295-26
[5] Computational protein stability analysis of SCN1A missense variants reveals domain-dependent stability patterns. — PubMed — Epilepsia open — https://doi.org/10.1002/epi4.70312
