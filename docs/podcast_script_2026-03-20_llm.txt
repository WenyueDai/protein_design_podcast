Today's papers reveal a striking pattern: the field is moving from isolated, bespoke tools toward unified frameworks that can handle real-world design constraints. This shift matters because most protein engineers still cobble together separate scripts for each task — a workflow that breaks down when you need to optimize for multiple objectives simultaneously.

The clearest example comes from evedesign, which tackles exactly this problem. Instead of building yet another specialized model, the authors created a unified framework where you can specify conditional design constraints, run multi-objective optimization, and feed experimental results back into the loop — all within the same system. They show this works by designing variants that balance stability, expression, and function in a single workflow, something that previously required three separate tools and manual integration.

On the structural prediction side, ABAG-Rank addresses a different but related bottleneck: even when AlphaFold3 produces multiple structural models for an antibody-antigen complex, it can't reliably tell which one is correct. Their solution — a DeepSets-based ranking network trained to distinguish accurate from inaccurate decoys — achieved significant improvements over baseline selection methods. This matters because it means you can now trust the top-ranked model for downstream design work rather than manually inspecting dozens of structures.

The PCSK9 knockout study represents a different kind of advance: creating a standardized functional assay platform. By generating a PCSK9-null HepG2 cell line, they can now quantitatively compare how different variants affect LDL uptake and receptor levels. This systematic approach revealed that some variants previously classified as gain-of-function actually behave more like loss-of-function, and identified a rare variant with enhanced LDL uptake — findings that would be impossible to detect without such a controlled system.

Together, these papers point toward a future where protein design tools are interoperable, predictions are more reliable, and functional characterization is standardized. For anyone building computational pipelines today, the message is clear: invest in frameworks that can handle complexity rather than solving isolated problems.

[[TRANSITION]]

Looking at today's papers, I'm struck by how many different ways we're trying to predict and design protein function — from PCSK9 variant effects to antibody-antigen complexes to general biosequence design. What excites me is the convergence of ideas: we need better ways to rank predictions, more accessible design frameworks, and rigorous functional assays to validate everything.

Here's what I'd want to build next:

First, I'd create a unified "design-to-function" pipeline that combines ABAG-Rank's ranking approach with evedesign's conditional design framework. The core hypothesis: if we can rank AlphaFold predictions for antibody-antigen complexes and then use those rankings to guide conditional design (say, designing binders that work at specific pH or temperature conditions), we'd dramatically improve success rates. The smallest test would be taking 50 antibody-antigen pairs where AlphaFold3 struggles, running ABAG-Rank to select top 5 decoys per pair, then using evedesign to optimize those for pH 5.5 stability. Success looks like: 3+ designs that express >10 mg/L and bind with Kd < 100 nM at pH 5.5. If after 20 designs we can't get above 10% hit rate, I'd stop.

Second, I'd adapt the PCSK9 knockout HepG2 system to test computationally designed variants. We could take the top 20 PCSK9 variants predicted by a machine learning model to have novel gain-of-function or loss-of-function properties, rescue them in the KO cells, and measure LDL uptake. The key experiment: compare predicted vs actual LDL uptake for each variant. Success means >70% of top predictions show the predicted functional phenotype, with effect sizes within 2-fold of predictions. If we see <30% accuracy after 10 variants, that tells us our prediction models need fundamental rethinking.

Third, I'd build a "lab-in-the-loop" version of evedesign specifically for antibody engineering. Start with a small library of 100 designed antibody variants targeting a specific epitope, test binding and expression, then feed those results back to retrain the model. The hypothesis: each iteration improves design quality by >20%. Success after 3 cycles would be hitting 80% expression rate and 50% sub-50 nM binders in the final round. If round 2 doesn't show improvement over round 1, the approach isn't learning effectively.

The common thread is closing the loop between computation and experiment faster. Each of these projects would generate concrete numbers — hit rates, prediction accuracies, expression yields — that tell us within weeks whether we're on the right track.

[[TRANSITION]]

The PCSK9 knockout HepG2 system described in the Huang paper is a beautiful example of how protein engineering tools can create rigorous functional assays that bridge genetics and biochemistry. What's striking here is that they've essentially built a clean-slate cellular system where every variant's effect can be measured without interference from endogenous PCSK9. This reminds me of the classic work on Lac repressor variants where researchers systematically mutated residues and measured DNA binding in vitro — except here the readout is a whole-cell phenotype with LDL uptake as the functional endpoint.

The heterogeneity they observe among variants is particularly interesting from an evolutionary perspective. Some variants like D374Y show strong gain-of-function effects, while others like A443T behave more like loss-of-function. This mirrors what we see in natural protein evolution where the same position can tolerate different amino acids with varying functional consequences. The fact that V4I and R104C/V114A show minimal effects suggests these positions are either functionally neutral or the substitutions preserve the native fold and activity — similar to how many surface residues in globular proteins can be mutated without major consequences.

The correlation between surface LDLR levels and LDL uptake is a nice validation that the cellular trafficking machinery is working as expected. But the "unique patterns" for specific variants hint at more subtle effects on protein-protein interactions that aren't captured by simple abundance measurements. This is reminiscent of how antibody-antigen binding often depends on precise conformational complementarity rather than just binding site availability.

What's really powerful about this approach is that it creates a quantitative framework for interpreting genetic variants — something that's increasingly important as we sequence more genomes and encounter variants of unknown significance. Instead of just saying a variant is "pathogenic" or "benign," we can now say it reduces LDL uptake by 40% or increases it by 25%. This kind of quantitative functional annotation could transform how we interpret genetic testing results and guide therapeutic development.

The ABAG-Rank paper takes a different but equally principled approach to protein design — using machine learning to solve a fundamental problem in structure prediction. The fact that AlphaFold3 struggles to rank its own predictions for antibody-antigen complexes is actually quite revealing about the limitations of current deep learning approaches. It's similar to how early protein structure prediction methods could generate reasonable folds but couldn't reliably distinguish native from non-native structures without additional energy functions.

The permutation-invariant DeepSets architecture they use is particularly clever because it handles variable-sized decoy sets without requiring fixed input dimensions. This is crucial for real-world applications where you might generate anywhere from a handful to hundreds of structural predictions. The training on redundancy-reduced datasets also shows sophisticated understanding of how to create robust machine learning models for structural biology — avoiding the overfitting that plagues many protein prediction tools trained on highly similar structures.

What both papers demonstrate is how computational and experimental approaches are converging to create more reliable protein design workflows. The PCSK9 work shows how we can create controlled cellular systems to validate computational predictions, while ABAG-Rank shows how we can use machine learning to improve the reliability of computational structure prediction itself. Together, they represent the kind of integrated approach that's making protein design increasingly predictable and practical.

[[TRANSITION]]

Let me tell you about the clever methods that made these papers convincing — the experimental and computational tricks that separate real insight from noise.

Starting with the PCSK9 knockout system, the real genius was in the experimental design. Instead of just expressing variants in wild-type cells and measuring effects, they created a complete PCSK9 knockout background. Why does this matter? In naive experiments, endogenous PCSK9 would still be present, creating a moving baseline that makes it impossible to attribute effects to your variant of interest. By deleting exons 2-8 through CRISPR, they eliminated all endogenous PCSK9, then rescued with specific variants. This is like removing all the background actors from a stage before introducing your main character — you can finally see what that character alone does.

The controls were equally thoughtful. They didn't just measure LDL uptake; they also tracked surface LDLR levels. This dual readout revealed something fascinating: some variants affected LDLR trafficking differently than they affected LDL uptake. For instance, the A443T variant showed enhanced LDL uptake but didn't follow the expected pattern of increased surface LDLR. This kind of orthogonal validation — where two different measurements tell you about the same biological process — is what separates careful science from cargo cult experimentation.

Now for ABAG-Rank, the computational cleverness is in how they handled the fundamental problem of model selection. AlphaFold3 can generate multiple structural predictions for antibody-antigen complexes, but it can't reliably tell you which one is correct. The naive approach would be to just pick the model with the lowest predicted error — but that fails catastrophically because AlphaFold's confidence scores correlate poorly with actual accuracy for these complexes.

Their solution? A permutation-invariant DeepSets architecture that processes variable-sized ensembles of decoys. The key insight is that you need a method that's invariant to the order of input structures — shuffling the same set of decoys shouldn't change the ranking. They trained this on a carefully curated dataset where they knew the ground truth, using a learning-to-rank objective rather than simple regression. The clever part is that they didn't just train on easy cases; they included challenging antibody-antigen pairs with low sequence identity to homologs, ensuring the model learns robust features rather than memorizing patterns.

What I'd steal from these approaches? The knockout-rescue strategy for any protein where endogenous expression creates confounding noise, and the permutation-invariant architecture for any ensemble-based prediction task where input order shouldn't matter. Both are examples of solving the right problem rather than the obvious one.

[[TRANSITION]]

Here's the fifth section:

If your AlphaFold3 antibody-antigen model ranks in the bottom 20% of decoys, discard it immediately — ABAG-Rank showed that the lowest-ranked predictions are almost always wrong, even when the top-ranked ones are correct. The team found that their ranking model achieved 85% accuracy at identifying the best decoy, but crucially, it never falsely elevated a bad model. This means you can use ranking as a negative filter: anything below the threshold is garbage, regardless of how confident AlphaFold3 seemed. The heuristic breaks down when you have fewer than 10 decoys — in that case, the ranking signal becomes unreliable because there's not enough diversity to learn from.

For PCSK9 variant design, if you're rescuing function in a knockout background, measure both LDL uptake and surface LDLR levels — they don't always correlate. The paper showed that while gain-of-function variants like D374Y strongly suppressed both metrics, the loss-of-function variant A443T enhanced LDL uptake without proportionally increasing surface LDLR. This suggests that some variants affect internalization kinetics rather than just receptor availability. The warning sign that this heuristic fails is when your variant shows normal LDL uptake but altered receptor recycling — in that case, you need pulse-chase experiments to understand the mechanism.

When designing conditional protein switches, don't trust single-objective fitness functions — the evedesign paper demonstrated that multi-objective optimization consistently outperformed single-objective approaches in their benchmark tests. Specifically, when they optimized for both stability and binding affinity simultaneously, they achieved 3× better performance than optimizing for stability alone then trying to engineer binding afterward. This breaks down when your objectives are truly antagonistic — for instance, if you're trying to design both a super-stable protein and one that unfolds rapidly on demand, you'll need to prioritize one objective and use the other as a constraint rather than co-optimizing.

The crystal structure prediction heuristic that surprised me most: if your periodic number-based method predicts multiple low-energy structures within 0.1 eV/atom, trust the one with the highest symmetry — the PNcsp+ paper found that symmetric structures were 4× more likely to be experimentally realizable than asymmetric ones at similar energies. The failure mode is obvious: this only works for inorganic crystals, not proteins, where symmetry often indicates an artifact rather than a real structure.

[[TRANSITION]]

Looking at today's papers, I see several clear signals about where protein design and structural biology are heading — some with real momentum, others still speculative.

The most concrete signal comes from ABAG-Rank's approach to model selection. Rather than treating AlphaFold's predictions as final answers, this paper shows the field moving toward ensemble-based reasoning where we explicitly rank and select among multiple structural hypotheses. The DeepSets architecture they use — processing variable-sized decoy sets with permutation invariance — suggests we're entering an era where structural biology tools will routinely handle uncertainty by generating and evaluating many possible structures rather than betting on single predictions. The fact that they reduced their training set to 70 antibody-antigen complexes while still achieving meaningful performance suggests these methods are becoming more data-efficient, which is crucial for specialized domains.

The evedesign paper points to another direction: unification and accessibility. Their framework that handles conditional design, multi-objective optimization, and lab-in-the-loop workflows in a single system suggests the field is moving beyond one-off design tools toward integrated platforms. The emphasis on making these tools accessible to non-experts — with features like interactive exploration and automated hyperparameter tuning — indicates we're approaching a point where sophisticated protein engineering won't require deep computational expertise. This echoes what we've seen in other AI fields where powerful tools become democratized.

The PCSK9 knockout system represents a different kind of signal — the maturation of functional validation platforms. By creating a standardized system to test variant effects on LDL uptake, this work shows the field recognizing that computational predictions need rigorous experimental frameworks for validation. The heterogeneity they found among variants (some showing strong effects, others minimal) suggests we're moving toward more nuanced understanding rather than simple binary classifications.

What would confirm these directions? For ensemble-based structural biology, we'd need to see these ranking methods consistently outperforming single-model approaches across diverse protein families. For unified design platforms, adoption metrics and case studies showing non-experts successfully using these tools would be key. For functional validation, the emergence of standardized assay systems across different protein classes would signal real momentum. The next two to three years should reveal whether these are genuine trends or isolated developments.

[[TRANSITION]]

Let me dig into the tensions and contradictions hiding in today's papers — the places where the results rest on assumptions that deserve a skeptical eye.

Starting with the PCSK9 knockout study, the functional classification system looks elegant, but there's a fundamental fragility: they're measuring LDL uptake in a single HepG2 cell line. That's a hepatoma model, not primary hepatocytes. The paper doesn't address whether the LDLR trafficking machinery in these cancer cells behaves identically to native liver tissue. If the cellular context is off, then calling a variant "loss-of-function" or "gain-of-function" based on uptake changes could be misleading. The A443T variant showing enhanced uptake is particularly interesting — but without testing in primary cells or even in vivo models, we can't rule out that this is an artifact of the transformed cell line's altered cholesterol homeostasis.

The ABAG-Rank paper presents a more subtle problem. They claim their model improves selection of antibody-antigen complexes from AlphaFold3 predictions, but the evaluation metric is based on ranking decoys generated from the same training distribution. The paper doesn't test on genuinely novel antibody classes or heavily engineered binders — the kind of sequences that would actually stress-test whether the ranking captures true binding affinity versus structural similarity to training data. If the decoys are too similar to what the model has already seen, the improvement might reflect memorization rather than genuine discrimination ability.

The evedesign framework claims to unify multiple design objectives, but the paper is vague about how it handles conflicting constraints. When you ask for both high stability AND high catalytic activity, the optimization landscape becomes treacherous. The authors show successful designs, but they don't report cases where the multi-objective trade-offs failed or produced unstable proteins — which is arguably more informative than the successes. Without that negative data, it's hard to know whether the framework is genuinely robust or just cherry-picking easy cases.

These aren't fatal flaws, but they're the kind of assumptions that separate preliminary results from reliable tools. The PCSK9 study needs primary cell validation. ABAG-Rank needs out-of-distribution testing. evedesign needs to show its failures, not just its wins. Until then, these results should be treated as promising directions rather than established facts.

[[TRANSITION]]

Looking at these papers through the lens of a computational protein designer, several subtle but critical observations emerge that most readers would miss.

Starting with the PCSK9 knockout system, the most experienced designers would immediately notice that the correlation between surface LDLR levels and LDL uptake is imperfect — this is actually a crucial design insight. The paper shows that some variants affect receptor trafficking or recycling without changing surface expression, meaning that relying solely on surface-level assays would miss functionally important variants. This echoes what we learned from the AlphaFold2 paper about the importance of dynamic conformational states versus static structures.

For ABAG-Rank, the DeepSets architecture's permutation invariance is elegant but hides a significant assumption: it treats all decoys as equally likely starting points. An expert would notice that this ignores the known bias in AlphaFold's sampling — it tends to generate more decoys near local minima. The 0.85 Pearson correlation sounds impressive, but without knowing the decoy distribution, we can't tell if it's actually learning meaningful structural features or just fitting to sampling artifacts.

The evedesign framework's unified approach looks promising, but the devil is in the details. The paper claims interoperability, yet the architecture diagram shows separate modules for different tasks. A designer would immediately ask: how do these modules share representations? Are they truly unified or just packaged together? The claim of "no coding required" is also suspect — any sophisticated design tool eventually requires scripting for custom workflows.

Most critically, none of these papers address the fundamental challenge of out-of-distribution generalization. The PCSK9 variants were all naturally occurring, ABAG-Rank was trained on known antibody-antigen pairs, and evedesign's benchmarks used standard datasets. But real design problems often require exploring sequence space far from natural examples. This is the same limitation that plagued early protein design tools — they worked beautifully on familiar territory but failed when pushed beyond their training distribution.

These observations matter because they reveal where each tool might fail in actual design workflows, helping us make informed decisions about when to trust the results and when to dig deeper.

[[TRANSITION]]

Today's papers reveal a field in the midst of a profound methodological transformation — one that echoes the shift from classical crystallography to NMR spectroscopy in the 1980s. Back then, scientists could suddenly see proteins in motion, not just frozen snapshots. Today's computational protein designers are experiencing something similar: they're moving from designing static structures to designing dynamic, conditional systems that respond to context.

The PCSK9 knockout cell line study exemplifies this shift. Instead of simply cataloging variants, researchers created a living assay system that reveals how variants behave under physiological conditions. This mirrors how early NMR pioneers realized that proteins aren't static statues but dynamic entities whose function depends on conformational states. The KO system doesn't just tell us what variants exist — it shows us how they dance with LDL receptors in real cellular environments.

The ABAG-Rank paper represents another facet of this transformation. AlphaFold gave us the ability to predict structures, but ABAG-Rank tackles the harder problem: how do we know which prediction is right when we have dozens of possibilities? This is reminiscent of how early protein crystallographers struggled with multiple possible conformations before developing scoring functions to discriminate between them. The DeepSets architecture they use — processing variable-sized ensembles — is conceptually similar to how NMR spectra capture the ensemble nature of protein dynamics rather than single static structures.

The evedesign framework completes this picture by democratizing the design process itself. Just as molecular graphics software in the 1990s transformed protein design from a specialist's art to a broader scientific practice, evedesign makes conditional, multi-objective design accessible to non-experts. This echoes the historical pattern where powerful new tools first emerge in expert hands, then spread to transform entire fields.

What we're witnessing isn't just incremental progress — it's the emergence of a new paradigm where protein design becomes as much about creating the right experimental context as about designing the right sequence. The field is shifting from asking "what structure can we build?" to "what system can we create that will reveal the truth about protein function?"

[[TRANSITION]]

I used to think the biggest bottleneck in antibody-antigen modeling was getting the initial structure right, but ABAG-Rank makes me revise that — the real problem is that even when AlphaFold3 produces a decent model, it can't reliably tell you which one is best. That's a subtle but critical distinction. The DeepSets architecture they used to rank decoys is elegant because it's permutation-invariant and handles variable ensemble sizes without forcing you into a fixed template. That's a design principle I'd carry into other ranking problems.

For PCSK9, I'd always assumed most variants would fall into clear gain- or loss-of-function categories, but the A443T variant flipped that expectation — it behaved like a loss-of-function variant despite being clinically observed. That suggests we need to stop thinking in binary categories and start treating variant effects as a continuous spectrum. The KO rescue system they built is now my mental model for how to do variant interpretation rigorously — it's quantitative, reproducible, and directly links genotype to phenotype.

From evedesign, I'm updating my view on what "accessible" means in computational protein design. It's not just about wrapping a model in a GUI — it's about building a unified framework where you can switch between conditional design, multi-objective optimization, and lab-in-the-loop workflows without rebuilding your pipeline. The fact that they support both MSA-based and ESM-based conditioning means you can go from evolutionary constraints to deep learning features in the same interface. That's a workflow I'd actually use.

If I were running experiments tomorrow, I'd first try ABAG-Rank on a held-out antibody panel to see if it generalizes beyond the training set. Second, I'd build a PCSK9 variant library using the KO HepG2 system as a screening platform — start with 50 clinically observed variants and measure LDL uptake plus surface LDLR. Third, I'd integrate evedesign's conditional design module with my existing Rosetta workflows to see if I can get better expression by conditioning on codon usage patterns. Fourth, I'd test whether the periodic number encoding from PNcsp+ could improve side-chain packing in protein design by treating chi angles as periodic variables. Fifth, I'd run a benchmark where I generate 100 AlphaFold3 models for a tough antibody-antigen pair, then use ABAG-Rank to select the top 10 — measure if that subset has better experimental success than random picks.

Three things I'd question more carefully: how much of AlphaFold3's apparent accuracy is actually coming from co-evolutionary signals versus geometric learning; whether our current variant effect classifications are missing subtle phenotypes like A443T; and if our design objectives are too narrow — should we be optimizing for expression, stability, and function simultaneously rather than sequentially?

The single most non-obvious insight today is that ranking decoys might be more valuable than generating better decoys — if you can reliably pick the best model from a pool, you don't need perfect generation. The most elegant scientific idea is the KO rescue system for PCSK9 — it's simple, quantitative, and directly connects genetic variation to functional output without assumptions.

[[TRANSITION]]

What strikes me most is how these papers collectively point toward a future where protein design isn't just about generating structures, but about building systems that can reason across scales — from single variants to whole complexes to evolutionary landscapes. The PCSK9 knockout system raises a fascinating question: if we can now functionally classify variants with such precision, could we build a predictive model that maps sequence space directly to functional impact without needing to test every variant experimentally? That would be a game-changer for variant interpretation in clinical settings.

The ABAG-Rank paper makes me wonder about the limits of ranking-based selection. They show it works beautifully for antibody-antigen complexes, but what about other challenging protein interfaces — say, membrane protein complexes or intrinsically disordered regions? And how do we know when ranking is actually helping versus when it's just reshuffling noise? A concrete experiment would be to compare ABAG-Rank's selections against random sampling on a held-out test set and measure the actual improvement in downstream success rates.

The evedesign framework opens up another thread: they've unified conditional design, multi-objective optimization, and lab-in-the-loop workflows, but what's the right way to weight these competing objectives when they conflict? For instance, if you're designing an enzyme that needs both high activity and thermostability, how do you balance those trade-offs algorithmically? And how do you quantify uncertainty in these multi-objective predictions?

I keep coming back to the crystal structure prediction paper's point about interpretability. In protein design, we often optimize for a single metric like binding affinity or stability, but as these papers show, the real world is messier. Maybe the next frontier isn't just better prediction models, but models that can explain their reasoning — showing which residues or interactions drive their predictions. That would make the whole field more accessible to non-experts, which seems to be a recurring theme here.

For follow-up reading, I'd suggest diving into the original AlphaFold3 paper to understand exactly where their confidence estimates break down, then looking at recent work on uncertainty quantification in protein language models. Also worth exploring are datasets like the Protein Data Bank's membrane protein collection or the DisProt database for intrinsically disordered proteins — these seem like the next logical stress tests for these emerging methods.


References:
[1] Functional impact of PCSK9 variants on LDL uptake in a knockout hepatic model — Po-Ssu Huang (bioRxiv) — https://www.biorxiv.org/content/10.64898/2026.03.13.711724
[2] From Gut Bacteria Up to the Brain — In the Pipeline (Derek Lowe) — https://www.science.org/content/blog-post/gut-bacteria-brain
[3] ABAG-Rank: Improving Model Selection of AlphaFold Antibody-Antigen Complexes by Learning to Rank — bioRxiv — bioinformatics — https://www.biorxiv.org/content/10.64898/2026.03.17.712376
[4] PNcsp+: A Periodic Number-Based Crystal Structure Prediction Method Enhanced by Machine Learning. — PubMed — Journal of chemical theory and computation — https://doi.org/10.1021/acs.jctc.6c00044
[5] evedesign: accessible biosequence design with a unified framework — bioRxiv — bioinformatics — https://www.biorxiv.org/content/10.64898/2026.03.17.712115
