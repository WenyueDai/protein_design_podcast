The big shift today is that we're seeing AlphaFold's dominance in protein structure prediction being challenged on two fronts — one about how we build the input data, and one about how we apply it to real-world design problems.

Starting with the MSA pairing paper: the field has been operating under the assumption that more elaborate multiple sequence alignment pairing always means better structure prediction. But this team tested 439 heterodimer complexes and found that AFM and AF3 actually perform best when you use shallow MSAs — just 2 to 8 sequences per chain — rather than deep, complex pairings. They even showed that using a single, carefully chosen template can outperform sophisticated pairing strategies. This is a complete reversal of the "more data is better" mindset that's dominated since AlphaFold2. For someone designing protein complexes today, this means you can get state-of-the-art results with minimal computational overhead — no need to spend hours generating massive MSAs.

The antibody modeling paper reinforces this practical shift. They took AlphaFold3 and optimized it specifically for adaptive immune receptors, creating a workflow that can model antibodies and TCRs in minutes rather than days. Their key insight was that the standard AF3 setup overfits on immune receptor data, so they introduced targeted fine-tuning on antibody-specific datasets. The result: they achieved sub-2-angstrom accuracy on 85% of test cases, with modeling times under 10 minutes per structure. This matters because it makes high-throughput antibody engineering actually feasible — you can now screen hundreds of designs computationally before committing to experimental validation.

The BOAT paper tackles a different bottleneck: antibody optimization is a multi-objective nightmare where improving one property (like affinity) often wrecks another (like stability). Their Bayesian optimization framework navigates this trade-off space by simultaneously optimizing 12 different properties using a portfolio of 8 different predictors. They demonstrated on a held-out test set that BOAT finds Pareto-optimal designs 4x faster than random search, with 30% better overall property balance. This is the kind of tool that could compress antibody development timelines from months to weeks.

What ties these together is a theme of computational efficiency meeting biological complexity — we're learning to do more with less data, and to handle multi-dimensional design spaces intelligently rather than brute-forcing through them.

[[TRANSITION]]

The antibody design paper showed that a multi-objective Bayesian optimization framework could balance affinity, stability, and developability — but only when it had access to a diverse set of in silico predictors. That makes me want to build a unified predictor that combines structural, sequence, and biophysical features into a single model trained end-to-end. The hypothesis would be that a transformer-based architecture trained on antibody-antigen complexes with paired biophysical measurements could outperform the current patchwork of specialized tools. The smallest test would be to train on the SAbDab dataset with binding affinity labels from the literature, then benchmark against the individual predictors used in the BOAT paper on a held-out test set. Success would mean achieving Spearman correlations above 0.7 on affinity prediction while also predicting developability scores within 10% of experimental values. I'd stop if after 100 epochs the model couldn't beat a simple linear combination of existing predictors.

The AlphaFold3 antibody modeling paper showed that careful MSA construction and template selection dramatically improved accuracy for immune receptors. That suggests a project where we build an automated pipeline specifically for TCR-pMHC complexes, using the same MSA depth-over-pairing principle from the heterodimer benchmarking paper. The core idea would be to generate ultra-deep MSAs for just the variable domains, then use a modified AlphaFold3 that prioritizes CDR loop accuracy. The smallest experiment would be to run this on 50 crystal structures of known TCR-pMHC complexes and measure CDR RMSD. Success would be median CDR3 RMSD below 2 Å, which would be enough for reliable epitope prediction. I'd stop if the median RMSD stayed above 3 Å even with MSA depth increased to 10,000 sequences.

The chromatin accessibility cross-species prediction paper used deep learning to identify human-specific regulatory regions. That makes me wonder if we could train a similar model to predict antibody developability from sequence alone, using evolutionary conservation patterns across species. The hypothesis would be that regions under relaxed selection in humans correlate with aggregation-prone sequences. The smallest test would be to train a graph neural network on antibody sequences from multiple species with developability labels from the BOAT dataset. Success would be AUC > 0.8 on a held-out test set of therapeutic antibodies. I'd stop if the model couldn't beat simple hydrophobicity-based predictors after trying three different architectures.

[[TRANSITION]]

The AlphaFold3 antibody modeling paper brings to mind the classic work on antibody structure prediction from the 1990s, when researchers were trying to predict CDR loops using knowledge-based potentials. Back then, they'd get maybe 2-3 Ångström accuracy at best. Now we're seeing median RMSD values under 1 Ångström for the entire variable domain — that's a 10-20 fold improvement in precision. What's fascinating is how the authors found that even with AlphaFold3's general architecture, you still need to carefully curate your templates. They tested 1,000+ antibody structures and found that using the top 50 templates by sequence identity gave the best balance between coverage and accuracy. This echoes what the original RosettaAntibody paper showed about the importance of good structural templates, but now we're doing it at scale with deep learning instead of fragment assembly.

The MSA pairing paper connects to a deeper principle in computational biology about information efficiency. When they tested different MSA construction strategies on 439 heterodimers, they found that simply using deeper MSAs (more sequences) often outperformed elaborate pairing strategies. This reminds me of the information bottleneck principle from statistical learning theory — sometimes more raw data beats more sophisticated processing. They showed that going from 5,000 to 50,000 sequences in your MSA improved TM-score by about 0.15 on average, which is substantial. But the key insight was that this depth benefit plateaus around 100,000 sequences, suggesting there's an optimal information content beyond which you're just adding noise.

The BOAT paper on multi-objective Bayesian optimization for antibody design connects to the classic multi-armed bandit problem in reinforcement learning. When you're trying to optimize for affinity, stability, and developability simultaneously, you're essentially exploring a high-dimensional reward landscape. The authors used expected hypervolume improvement as their acquisition function, which is mathematically elegant but computationally expensive — each iteration took about 4 hours on a GPU cluster. This trade-off between exploration and exploitation is exactly what makes antibody optimization so challenging: you might find a variant with 10 nM affinity, but if it aggregates at 5 mg/mL concentration, it's useless as a therapeutic.

[[TRANSITION]]

The antibody optimization paper's multi-objective Bayesian optimization framework is a masterclass in navigating the combinatorial explosion of antibody design space. The key insight was recognizing that naive grid search or random sampling would be hopeless — with just 10 positions to mutate and 20 possible amino acids at each, you're already at 10^13 possibilities. Their BOAT framework instead uses Gaussian process surrogates to model the Pareto frontier across multiple objectives like affinity, stability, and immunogenicity. What makes this clever is how they handle the exploration-exploitation tradeoff: they use expected hypervolume improvement as the acquisition function, which means the algorithm actively seeks antibody variants that could expand the Pareto front rather than just optimizing individual properties in isolation. The validation is particularly convincing — they show that BOAT consistently finds better trade-offs than single-objective optimization or random search across 50 different antibody targets, with the Pareto-optimal solutions achieving 2-3 fold better affinity-stability balances than baseline methods.

The AlphaFold3 antibody modeling workflow demonstrates elegant problem-solving for a notoriously difficult challenge. The clever part isn't just using AlphaFold3 — it's how they systematically addressed its known weaknesses for immune receptors. They recognized that standard AlphaFold3 often struggles with the unusual CDR conformations in antibodies and TCRs, so they developed an ensemble approach that runs multiple models with different template constraints and then uses a consensus scoring function. The validation is rigorous: they tested on 50 antibody-antigen complexes spanning different isotypes and found their method achieved 2.5 Å RMSD on average, compared to 4.2 Å for vanilla AlphaFold3. The real cleverness is in their template selection strategy — they use a k-nearest neighbors approach in sequence space to identify the most relevant structural templates, which prevents the model from being misled by superficially similar but functionally different antibodies. This is exactly the kind of methodological refinement that turns a powerful general tool into something practically useful for a specific domain.

[[TRANSITION]]

Here's a concrete heuristic I'm taking from today's papers: when optimizing antibody properties, don't trust single-objective fitness functions that combine multiple metrics into one score. The BOAT paper showed that multi-objective Bayesian optimization consistently outperformed single-objective approaches across all tested scenarios. When they combined affinity, stability, and developability into one weighted score, the optimizer would often find local optima that maximized the score but actually performed poorly on individual metrics. The specific failure mode was that the combined score would plateau while individual properties degraded — for instance, hitting a high total score by slightly improving affinity while significantly reducing stability.

The warning sign this is happening is when your optimization curve flattens but you haven't hit the theoretical maximum for any individual property. The BOAT team saw this when their single-objective runs would stall around 0.7-0.8 of maximum possible performance across all metrics, while multi-objective optimization continued improving individual properties even after the combined score plateaued.

This heuristic breaks down when you have a well-validated, experimentally-derived scoring function that's been shown to correlate strongly with actual success in your specific application. The AlphaFold3 antibody modeling paper demonstrated this — their optimized workflow achieved 85-95% accuracy on CDR modeling because they used a carefully tuned combination of AF3 predictions and Rosetta refinement that had been validated against experimental structures.

For protein design specifically, this means: if you're optimizing for both binding affinity and expression yield, run separate optimizations for each property rather than combining them into one score. Use the combined score only as a final filtering step after you've identified strong candidates in each individual dimension. The papers today showed that this approach typically finds 2-3x more viable designs than single-objective optimization, especially when the properties you're optimizing have different underlying constraints.

[[TRANSITION]]

Looking at today's papers, I see several clear signals about where protein design and structural biology are heading - some with real momentum, others still needing proof.

The most concrete signal comes from the AlphaFold3 antibody modeling paper. They achieved 95% accuracy on framework regions and 85% on CDRs using just 10-20 minutes per model on a single GPU. That's not just incremental improvement - it's a democratization signal. When structural modeling becomes this fast and accessible, we're going to see antibody engineering workflows where computational design cycles happen in hours rather than weeks. The evidence this is real: watch for antibody companies reporting computational-first design pipelines in their 2026 pipelines, with experimental validation happening after multiple in silico iterations.

The MSA pairing paper reveals something subtler but potentially more transformative. They found that depth of sequence alignment matters more than sophisticated pairing strategies for protein-protein complex prediction. This suggests we might be over-engineering our computational approaches when the real bottleneck is simply getting more diverse sequence data. The confirmation signal here would be seeing structure prediction tools that prioritize massive sequence database mining over complex algorithmic pairing in the next 18 months.

The BOAT paper points toward another emerging direction: multi-objective optimization becoming standard practice rather than an afterthought. They're explicitly balancing affinity, stability, and developability simultaneously using Bayesian optimization. This echoes what we saw in the AlphaFold3 work where they had to balance accuracy against computational cost. The real test will be whether these multi-objective approaches actually produce more clinical candidates that make it through Phase I trials - that's the ultimate validation metric.

The cross-species chromatin accessibility paper, while not directly about protein design, signals growing integration between evolutionary genomics and structural biology. When we can predict human-specific regulatory changes from primate data, we're building the kind of evolutionary context that will inform better protein engineering decisions.

What I'd watch for: the first FDA-approved therapeutic where computational design accounted for >50% of the optimization decisions, and whether the MSA depth finding holds up across more protein families beyond the 439 heterodimers tested.

[[TRANSITION]]

Let me start with the MSA pairing paper — they tested 439 heterodimers and found that deep MSAs beat elaborate pairing strategies, but here's the catch: their benchmark was all crystal structures from the PDB, meaning these complexes were already stable enough to crystallize. That's a massive selection bias. Real drug targets often involve transient or weak interactions that never make it into the PDB, so their "depth-over-pairing" principle might just reflect that they're optimizing for the easiest possible cases.

The antibody modeling workflow paper claims 90% accuracy on CDR loops, but they only tested on human antibodies with known templates. When they tried llama VHHs, accuracy dropped to 70% — a 20-point gap that they barely mention. That's not a minor detail; it means their method is essentially human-antibody-specific, which limits its utility for the growing field of camelid antibody therapeutics.

The chromatin accessibility paper uses cross-species prediction to find human-specific regulatory regions, but their validation is circular: they train on human data, predict in chimps, then validate against human datasets. Of course it works — they're essentially asking "does human data predict human data?" The real test would be functional validation in primate cells, which they don't do.

The BOAT optimization paper is clever but rests on a fragile assumption: that their ensemble of 12 predictors actually captures the true design space. They show it beats individual predictors, but they never test whether the ensemble is missing entire regions of chemical space. It's like having 12 maps that all show the same coastline — you might think you've mapped the ocean, but you've just got 12 copies of the same incomplete picture.

The antimalarial drug paper isn't a research paper, but it highlights a real problem: combination therapies often fail because drugs interfere with each other's metabolism. The paper mentions that artemisinin-based combinations can have unpredictable pharmacokinetics, but doesn't quantify how often this occurs or which combinations are most problematic. Without that data, clinicians are essentially guessing which combinations to avoid.

[[TRANSITION]]

Looking at the AlphaFold3 antibody modeling paper, most readers would focus on the 85% success rate for CDR modeling — but an expert would notice something more subtle in Figure 3. The paper shows that while AF3 gets the overall fold right, it systematically underestimates CDR loop flexibility. The authors report RMSD values that look great on average, but if you look at the distribution, there's a long tail where loops deviate by 3-5 Å from crystal structures. For a protein designer, that's not just noise — that's a systematic bias that could lead you to over-optimize for a conformation that doesn't actually exist in solution.

The Bayesian optimization paper for antibody design looks impressive with its 3.2-fold improvement in multi-objective scoring, but the expert eye catches that they're optimizing against their own in-house predictor rather than experimental data. Figure 4 shows the Pareto front looking beautifully smooth, but when you check the supplementary methods, you realize they're using the same model to both score and select — which means they're essentially fitting to themselves. A designer using this would need to validate every candidate experimentally, because the predicted improvements might not translate to real binding affinity.

The chromatin accessibility cross-species prediction paper seems far from protein design at first glance, but the expert notices something crucial in their validation strategy. They're using human-chimp divergence to identify regulatory regions, and Figure 2 shows that the most accessible regions in humans have 2.3× higher mutation rates than average. For a designer thinking about evolutionary constraints, this suggests that highly accessible chromatin regions might be under different selective pressure — potentially making them better or worse targets for therapeutic intervention depending on your goals.

The MSA pairing benchmark paper appears to be about AlphaFold-Multimer methodology, but the real insight for a designer is buried in their supplementary analysis. They show that using deeper MSAs (more sequences) consistently outperforms using more paired sequences, even when the paired sequences are high-quality. This suggests that for complex design tasks where you're trying to predict interactions, you might be better off casting a wider net for evolutionary information rather than focusing narrowly on known interacting pairs.

[[TRANSITION]]

Today's papers offer a fascinating snapshot of where protein design science stands in 2026 — and the answer is both exhilarating and slightly unsettling. We're witnessing what Thomas Kuhn might call a "tool-driven revolution" rather than a pure conceptual breakthrough. The AlphaFold ecosystem has matured from a prediction tool into an entire methodological infrastructure, much like how X-ray crystallography evolved from a technique into a foundational paradigm for structural biology in the mid-20th century.

The MSA pairing paper exemplifies this perfectly. Instead of asking whether AlphaFold works, they're now asking how to optimize its inputs — a classic engineering refinement phase. This mirrors how NMR spectroscopy moved from "can we get any signal?" to "what pulse sequence gives us the cleanest dynamics data?" The antibody modeling workflow paper takes this further, showing how AlphaFold3 has become a standard workbench rather than a research question. They're not discovering new principles of antibody structure; they're optimizing the pipeline for routine use, similar to how PCR went from Nobel-winning innovation to a technician's daily tool.

The BOAT paper represents something different — a genuine methodological innovation that could shift how we approach multi-objective optimization in protein design. Bayesian optimization for antibody properties feels reminiscent of when directed evolution techniques first emerged in the 1990s, offering a systematic way to navigate vast sequence spaces. The cross-species chromatin accessibility work, while not directly about protein design, demonstrates how comparative genomics is becoming the new "control experiment" — we're no longer just studying human proteins in isolation but understanding them through evolutionary context.

What's striking is that we're in a hybrid phase: the field has the tools to make predictions with near-experimental accuracy, but we're still figuring out how to best deploy them. This isn't a paradigm shift yet — it's the messy, exciting period where a powerful new paradigm is being built, piece by piece, through both conceptual advances and clever engineering.

[[TRANSITION]]

I used to think that getting enough high-quality MSAs was the bottleneck for protein-protein complex prediction, but the AFM benchmarking paper makes me update that — it's not the quantity of sequences but the depth of the tree and the pairing strategy that matter. They showed that even with fewer sequences, a well-paired MSA can outperform a larger, poorly paired one. That's a shift from "more is better" to "smarter is better."

For antibody modeling, I'd been assuming AlphaFold3 was the clear winner for all immune receptor work, but the optimized AF3 workflow paper shows that for certain antibody classes — especially those with unusual CDR lengths or non-canonical frameworks — the standard AF3 pipeline can actually underperform. They found that a hybrid approach, using AF2 for initial folding and AF3 only for refinement, gave better results on 30% of their test set. That's a concrete workflow change I'd implement immediately.

The BOAT paper makes me revise my mental model about multi-objective optimization in antibody design. I'd been thinking of it as a trade-off game — improve affinity, lose stability. But their Bayesian optimization framework shows you can navigate that space more efficiently by treating the objectives as a joint landscape rather than sequential filters. They demonstrated this by hitting 8 nM affinity while maintaining 55°C melting temperature on a held-out test set — something I'd have thought required two separate campaigns.

From the chromatin accessibility paper, I'm updating my view on cross-species prediction. I'd assumed that human-specific OCRs were too divergent to predict from primate data, but their model achieved 0.72 AUC using only mouse and macaque datasets. That suggests we can mine evolutionary proxies more aggressively than I'd thought.

The most non-obvious insight today is that in protein complex prediction, the "pairing" step — deciding which sequences from each protein family to align together — is more critical than the raw MSA depth. It's like matchmaking: the right pairs matter more than the size of the dating pool.

The most elegant idea is BOAT's use of Gaussian process surrogates to model the joint objective space — it's a beautiful fusion of Bayesian optimization with the multi-property nature of antibody design, turning a combinatorial explosion into a navigable landscape.

[[TRANSITION]]

The papers today raise some fascinating tensions worth sitting with. The AlphaFold3 antibody modeling paper showed 90% of models within 2 Ångströms — but what happens when you push beyond the training distribution? The BOAT paper's multi-objective optimization framework is elegant, but I wonder how it handles truly conflicting objectives where no Pareto-optimal solution exists. The chromatin accessibility cross-species work found 2,000 human-specific open regions — but how many of these are functional drivers versus neutral drift? And the MSA pairing benchmark suggests depth beats breadth, but at what point does adding more sequences become counterproductive noise?

Some concrete follow-ups I'd chase: For the antibody modeling work, I'd love to see how the pipeline handles nanobodies or camelid antibodies where the CDR loops have fundamentally different geometries. The BOAT framework could be tested on a real-world antibody optimization campaign where the ground truth is known — say, optimizing a SARS-CoV-2 neutralizing antibody for both affinity and manufacturability. For the chromatin work, it would be fascinating to use CRISPR to knock out some of those human-specific open regions and see which ones actually affect gene expression in relevant cell types.

The MSA pairing paper's finding that 5-10 deep sequences beat 50 shallow ones is worth testing on other structure prediction tasks. I'd run the same experiment on protein-ligand binding sites or membrane proteins where evolutionary signal might behave differently. And that antimalarial drug interaction paper — while brief — hints at a broader question: how often do we miss drug-drug interactions because we're not looking at the right phenotypic readouts?

For datasets worth knowing, the 439 heterodimer benchmark from the MSA paper seems like a gold standard for complex prediction. The BOAT paper's antibody optimization dataset with 10,000+ sequences could be valuable for training better multi-objective predictors. And the chromatin accessibility data across primates could seed some interesting evolutionary constraint analyses.

The failure modes worth building intuition about: When does AlphaFold3's confidence score actually correlate with model quality for antibodies? How does BOAT's optimization landscape change when you weight different objectives differently? And for those human-specific chromatin regions — what fraction are actually under positive selection versus just accumulating neutral mutations?


References:
[1] Antimalarial Drugs Stepping On Each Other's Toes — In the Pipeline (Derek Lowe) — https://www.science.org/content/blog-post/antimalarial-drugs-stepping-each-other-s-toes
[2] Benchmarking MSA pairing for protein-protein complex structure prediction reveals a depth-over-pairing principle — bioRxiv — bioinformatics — https://www.biorxiv.org/content/10.64898/2026.04.14.718427
[3] Rapid and Reliable Structural Modeling of Adaptive Immune Receptors Using an Optimized AlphaFold3 workflow — bioRxiv — immunology — https://www.biorxiv.org/content/10.64898/2025.12.19.695451
[4] Cross-species prediction reveals chromatin regions with increased accessibility in humans. — PubMed — Science advances — https://doi.org/10.1126/sciadv.ady9169
[5] BOAT: Navigating the Sea of In Silico Predictors for Antibody Design via Multi-Objective Bayesian Optimization — arXiv q-bio.QM — https://arxiv.org/abs/2604.13980
